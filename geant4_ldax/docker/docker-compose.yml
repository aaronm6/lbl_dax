version: '3.4'
services:
  dockerfile:
    build:
      context: .
      args:
        USERNAME: ${USER}
    image: hydrox_geant4
    stdin_open: true
    tty: true
    container_name: hydroxgeant4
    network_mode: "host"
    ipc: host
    privileged: true
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - $HOME/.Xauthority:/home/${USER}/.Xauthority
      - ../Geant4_Sim:/tools/Geant4_Sim:rw
    devices:
      - /dev/dri:/dev/dri
    environment:
      - "DISPLAY=$DISPLAY"
    command: >
      bash -c "chown -R ${USER}:${USER} /tools/Geant4_Sim && exec bash"
